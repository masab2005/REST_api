import test from "node:test"
import assert from "node:assert"
import fetch from "node:fetch"

const BASE_URL = "http://localhost:5001/api";

test("does not get access to authenticate routes without token", )